{
  "name": "@demiurge/web",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "test": "vitest run"
  },
  "dependencies": {
    "@demiurge/shared": "workspace:*",
    "@demiurge/wasm-sim": "workspace:*",
    "three": "^0.181.0"
  }
}
